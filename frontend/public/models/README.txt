Place your anime VRM model here as:

anime.vrm

Or set a custom URL in .env:
VITE_VRM_MODEL_URL=/models/your-model.vrm

Optional second model for the model-switch button:

alt-model.fbx

Or configure it in .env:
VITE_ALT_MODEL_URL=/models/your-second-model.fbx
VITE_ALT_MODEL_LABEL=Second Model
